
ubuntu、new_bmv2、new_ryu、ubuntu_ai、quagga、ovs


scp -P 4500 lzl@192.168.3.15:~/tmx/Nika/src/net_env/ubuntu_ai.tar D:\

scp D:\ubuntu_ai.tar adminis@192.168.1.33:~/images/


我有一项基于LangChain/LangGraph和实验室网络仿真平台klonet的LLM故障诊断智能体以及基础测评项目。
文件夹net_env存放着7种基础网络场景的拓扑创建代码(代码名就是拓扑名，除了 base_netenv.py表示基类)，文件夹utils的llm_models.py存放着调用大模型以及rag_milvus.py存放着故障注入智能体的RAG模块，文件夹 mcp_server 存放着与klonet交互的基础API代码klonet_base_api.py和封装着MCP工具的klonet_server.py。MCP工具包括节点内执行指令、ping等等基础工具，而fault_inject.md则是故障注入智能体用来RAG的文档()。目前故障注入智能体以及评价智能体已经完成了。之前的故障诊断智能体设计为一个 React 智能体，可以调用非常专业的MCP工具(这些工具和之前的28种故障是一一对应的而且包含正则匹配等辅助agent的提示输出所以很死板)，用户输入故障位置和表现，如"h1不能 ping 通192.168.3.11"，agent多次调用后，提交归因(也就是故障名称)。现在的任务升级，用户输入模糊的投诉信息，如"业务高峰期，用户反馈请求 AI 接口时常报错或响应时间长到无法忍受。"，不告知 agent 故障具体位置和根本原因。需要 agent 先全局 ping，然后诊断整个拓扑哪里不通，为什么不通。最后提交故障位置节点和故障归因。所以需要升级成多智能体系统，涉及到 workflow 编排，并且增加 RAG (Milvus数据库)和 长期记忆模块(MySQL)，截取经验丢给智能体。有些工具输出可能会很长(如 BGP summary)，所以需要格外的智能体专门处理工具输出，截断输出内容并增加hint再丢回给出事的智能体。注意：klonet_server.py 是 故障诊断智能体(集群)和故障注入智能体共用的，分别在于不同的记忆和RAG文档。请你上网查阅 LangChain/LangGraph 文档以及网络故障诊断经验，并且给出新的diagnose_agent.py的架构设计，先别给代码！给出架构设计和详细的方案！

【2】main.py 需要对接四个板块：网络场景部署 -> Human_in_Loop(是销毁拓扑和重新部署(用户输入部署的网络场景)还是开始注入故障(输入故障需求)) -> 网络故障注入 -> Human_in_loop(是重新注入故障(输入故障需求)，还是开始诊断(提示用户选择 LLM 模型、输入最大执行次数)) -> 故障诊断 -> Human_in_loop(是重新诊断，还是开始测评(选择测评的 LLM 模型)) -> 测评 -> 结束(删除拓扑)。
# 删除拓扑代码
```py
from mcp_server.klonet_base_api import KlonetBaseAPI 
# 5. 销毁拓扑
print(f"\n[System] 🧹 正在销毁网络拓扑 ({lab_name})...")
try:
    api = KlonetBaseAPI(lab_name)
    api.lab.reset_project()
    print("[System] ✅ 拓扑销毁成功。")
```
需要你重写 agent/main.py，其他四个代码(network_deploy_agent.py、judge_agent.py、fault_inject_agent.py、diagnose_agent.py)都放在 agent 目录下，给出包含详细注释的完整代码。接着在 agent/test 子目录下写一份测试脚本 ultimate_test.py，系统测试全部 58 种故障。这个脚本需要能把每一次诊断结果保存在 agent/test/results 目录下，名字为<故障名>.log。




docker build -t ubuntu_ai_server:latest .
docker build -t ubuntu_ai_client:latest .

docker save -o new_quagga.tar new_quagga:latest

docker build --network=host \
  --build-arg http_proxy=http://127.0.0.1:10808 \
  --build-arg https_proxy=http://127.0.0.1:10808 \
  --build-arg HTTP_PROXY=http://127.0.0.1:10808 \
  --build-arg HTTPS_PROXY=http://127.0.0.1:10808 \
  -t new_ubuntu:latest .



（1）链路故障（4种）（2）主机故障（9种）（3）FRR故障（6种）（4）BGP故障（6种）（5）OSPF故障（7种）（6）RIP故障（7种）（7）P4故障（5种）（8）SDN故障（8种）（9）AI 推理故障(共6种)











# 劫持 cd 命令（只管代理，不管虚拟环境了）
cd() {
    builtin cd "$@" && auto_switch_proxy
}

# 初始加载时执行一次代理判定
auto_switch_proxy

# ==========================================
# 2. 虚拟环境记忆与管理 (持久化锁定)
# ==========================================
# 自定义命令：用于手动切换并记忆虚拟环境
set_env() {
    local target_env=""
    case "$1" in
        "nika"|"Nika")
            target_env="/home/lzl/tmx/Nika/.venv"
            ;;
        "pg"|"PlayGround")
            target_env="/home/lzl/tmx/PlayGround/.venv"
            ;;
        "default"|"tmx")
            target_env="/home/lzl/tmx/venv"
            ;;
        *)
            # 如果输入的是绝对路径，也支持
            target_env="$1"
            ;;
    esac

    # 检查该环境的 activate 脚本是否存在
    if [ -f "$target_env/bin/activate" ]; then
        # 1. 将路径写入本地记忆文件
        echo "$target_env" > ~/.last_venv
        # 2. 使用标准的 source 激活它（这比强行修改 PATH 更干净，也能显示命令行前缀）
        source "$target_env/bin/activate"
        echo -e "\033[32m[OK] 已激活并锁定虚拟环境: $target_env\033[0m"
    else
        echo -e "\033[31m[Error] 找不到虚拟环境: $target_env\033[0m"
    fi
}

# 终端每次启动时，读取记忆文件并自动激活
if [ -f ~/.last_venv ]; then
    LAST_ENV=$(cat ~/.last_venv)
    if [ -f "$LAST_ENV/bin/activate" ]; then
        source "$LAST_ENV/bin/activate"
    fi
else
    # 第一次运行没有任何记忆时，默认回退到你的全局基础环境
    set_env default > /dev/null
fi

















我希望把这个RAG模块作为一个fault_inject_agent.py中的子节点叫 Fault_Inject_Hint，这个召回结果加上拓扑信息作为系统提示词，传给一个React agent 直到它认为注入成功了(设置时间、工具步骤限制)。然后整个fault_inject_agent.py的 Fault_Inject_Agent 作为这个项目的一个子节点，和 Human_in_Loop、Diagnose_Agent、Evaluator一起接入 LangGraph的图。


（1）我希望召回时使用混合检索，也就是向量检索+BM25 稀疏检索（传统的关键字匹配）召回出Top3答案，再通过重排(你需要给出最佳的重排方式)，选取出Top1。
（2）下载好的 嵌入模型和重排模型 需要放在utils/rag_models文件夹下，而不是缓存(容易被误删)


每次连接虚拟机，Vscode右下角弹窗显示：网络出现错误，请检查网络是否通畅，或单击了解更多获取更多帮助。怎么回事？实际上并没有问题。

export HF_ENDPOINT=https://hf-mirror.com
uv run src/utils/rag_milvus.py



请你参考我的github上的网络故障排除智能体以及基准测试平台项目，按照如下修改：

我的win11电脑在VsCode远程连接linux虚拟机(也就是代码所在地)。
新方案的整个项目tree：
├── KlonetAPI
│   ├── __init__.py
│   ├── klonet_api
│   └── klonet.py
└── src
    ├── agent
    │   ├── fault_inject_agent.py
    │   ├── __init__.py
    │   ├── judge_agent.py
    │   ├── main.py
    │   ├── react_agent.py
    ├── app.py
    ├── mcp_server
    │   ├── __init__.py
    │   ├── klonet_server.py
    ├── net_env
    │   ├── base_netenv.py
    │   ├── ospf_enterprise.py
    │   ├── p4_star_code.p4
    │   ├── p4_star.py
    │   ├── rip_internet.py
    │   ├── sdn_openflow.py
    │   ├── simple_bgp.py
    │   └── static_routing.py
    ├── orchestrator.py
    └── utils
        ├── __init__.py
        └── llm_models.py
一、KlonetAPI：核心理解 klonet.py就行，通过http请求管理Klonet上的仿真网络拓扑，全部保留
二、src：
（1）src/utils：通用模块
llm_models.py：封装远程/本地 LLM，
（2）src/service：服务模块
(a) 原来的 klonet 文件夹：基于 KlonetAPI 封装一系列控制 API，需要废弃
(b) 原来的 mcp_server 文件夹：只保留一台 klonet_server.py，工具全部改造为在节点容器内输入指令的工具，因此只需要ubuntu、ovs、bmv2、frr、ryu容器输入指令工具。参数为节点名字+指令。这些工具既可以用来诊断故障，也可以用来注入故障。
（3）src/net_env：klonet 平台创建网络拓扑
只需要增加一种场景：AI_inference(AI推理)，其他完全保留
（4）src/fault_injector：基于 KlonetAPI 封装向klonet平台的网络拓朴注入故障
之前的方案：分为主机测故障、服务故障(frr 路由和协议)、链路故障三种，写入指令注入
现在的方案：废除这个文件夹
（5）src/agent：智能体系统
之前的方案：react_agent.py 负责 诊断故障原因并提交，接着系统给出客观评价，然后dudge_agent.py 负责 评价react_agent诊断表现。三个模块使用main.py基于 LangGraph 状态图打包。
现在的方案：
任然基于 LangGraph/LangChain，实现：
(a) 故障注入智能体 fault_inject_agent：使用 本地部署模型 + RAG + CoT + MCP Tool Call，实现当用户输入故障名字时，自动化故障注入，每一次故障注入之后，还有检测模块检测注入是否成功
(b) 故障诊断智能体集群 diagnose_agent.py：重新设计架构，首先是一个全局 ping 测试智能体，他负责全局诊断哪里 ping 不通（节点和 ）；然后报告交给 react 智能体诊断(主体，结合网络环境、用户投诉和之前的表现，思考并给出下一步工具)；在设计一个工具调用解析智能体，它和react智能体打配合，能根据react 智能体的指示截取工具部分原始内容并给出“迹象”警告然后返回给 react 智能体。最终输出诊断结果：包含故障节点名字(node_name)+故障原因(RCA)
(c) 评价智能体 judge_agent.py：输入诊断流程，给出评分和理由，无需修改！
(d) 主函数模块 main.py，打包项目
（6）orchestrator.py：主函数，负责组织项目：创建拓扑->注入故障->诊断故障并评价->摧毁拓扑
（7）app.py：基于Flask的前端。

我需要从 huggingface 上使用 vLLM 本地化部署 LLM，需要包含工具调用、结构化输出等能力，先给出这个教程！
